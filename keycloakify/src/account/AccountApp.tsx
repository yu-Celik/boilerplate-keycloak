// AccountApp — placeholder for Phase 6 (account theme migration).
// Currently uses the default keycloakify account rendering via KcPage.tsx.
// Phase 6 will add custom Tailwind+shadcn account pages here.
import "../shared/theme.css";
